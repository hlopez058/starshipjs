var game = {
    player:null,
    canvas:null,
    score: null,
    controller:null,
    objects:null,
    create : function(){
      var obj = Object.create(this);
      obj.player = null;
      obj.canvas = null;
      return obj;
    },
  
    //add the game state information
    //add the player state information
    //add the canvas information
  }